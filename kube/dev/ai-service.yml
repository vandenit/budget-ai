apiVersion: v1
kind: Service
metadata:
  name: budget-ai-svc
  namespace: dev
  labels:
    app: budget-ai-app
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: budget-ai-app
