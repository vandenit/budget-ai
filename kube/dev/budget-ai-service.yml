apiVersion: v1
kind: Service
metadata:
  name: budget-ai-svc
  namespace: dev
  labels:
    app: budget-ai-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: budget-ai-app
