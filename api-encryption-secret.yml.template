apiVersion: v1
kind: Secret
metadata:
  name: budget-api-encryption-secret
  namespace: dev
type: Opaque
data:
  # Generate with:
  # openssl genrsa -out server-private.pem 2048
  # openssl rsa -in server-private.pem -pubout -out server-public.pem
  # cat server-private.pem | base64 -w 0
  # cat server-public.pem | base64 -w 0
  SERVER_PRIVATE_KEY: ${SERVER_PRIVATE_KEY_BASE64}
  SERVER_PUBLIC_KEY: ${SERVER_PUBLIC_KEY_BASE64}
