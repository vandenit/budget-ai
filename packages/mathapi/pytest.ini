[pytest]
# Disable deprecation warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning

# Configure test output
addopts = 
    --verbosity=2
    --no-header 
    --tb=short 

# Test paths
testpaths =
    app/tests 