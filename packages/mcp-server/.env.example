# API Mode (default and recommended)
# Get your Auth0 token from Claude Desktop or YNAB web app
API_BASE_URL=http://localhost:4000
AUTH0_TOKEN=your_auth0_jwt_token_here

# Database Mode (legacy - direct MongoDB access)
# Only use if you want to bypass the API
# MONGODB_URI=mongodb://localhost:27017/budget-ai

# Optional: Set log level
# LOG_LEVEL=info
